# Jarvis 桌面助手 V1 —— 最终交付报告

> 交付日期：2026-09-21
> 交付形态：**可安装的 Windows EXE + 完整源码 + 全套文档 + 全套验收证据**
> 交付位置：`C:\开发任务\Jarvis\`
> 本报告所有数据均为**实际执行输出**，可复现命令见各节

---

## 一、最终验收结果（全部实测通过）

| 验收项 | 结果 | 复现命令 |
|---|---|---|
| TypeScript 类型检查 | **0 错误** | `node scripts/run.mjs typecheck` |
| 完整构建（4 步） | **通过** | `node scripts/run.mjs build` |
| 应用自测（开发态，含 T07 扩展项） | **失败 0 / 全部通过** | `node scripts/run.mjs selftest` |
| 应用自测（**打包后**） | **失败 0 / 全部通过** | `release\win-unpacked\Jarvis.exe --selftest` |
| **真实实时语音链路验收** | **通过 13 / 失败 0** | `node scripts/verify-realtime.mjs` |
| **应用内真实端到端联调** | **通过 11 / 失败 0** | `node scripts/verify-app-e2e.mjs` |
| MCP 协议端到端（开发态） | **通过 5 / 失败 0** | `node scripts/verify-mcp.mjs` |
| MCP 协议端到端（打包态） | **通过 3 / 失败 0** | 见 `docs/evidence/packaged-mcp.log` |
| T07 单元测试 | **通过 69 / 失败 0** | `T07.../node_modules/.bin/vitest run` |
| T04 状态机测试 | **通过 15 / 失败 0** | `T04.../app && npm test` |

**安装包**：`release/Jarvis-Setup-1.0.0-x64.exe`（119 MB）

---

## 二、真实 API 联调结果（本次接上密钥后的关键进展）

### 2.1 打通的能力

用真实 StepFun API Key 完成：

| 能力 | 结果 |
|---|---|
| API 鉴权 | ✅ 通过（`Authorization: Bearer`） |
| 文本对话 | ✅ 通过（回复"你好"） |
| **视觉图像理解** | ✅ 通过 —— 生成 32×32 纯红 PNG，模型正确识别为"**红色**" |
| **实时语音握手** | ✅ 通过（`session.created` → `session.updated`） |
| **服务端 VAD 断句** | ✅ 通过（`speech_started` + `speech_stopped` 均正常推送） |
| **音频往返** | ✅ 通过 —— 收到 **13 个音频分片 / 89 KB** 真实 PCM |
| **打断（response.cancel）** | ✅ 通过 —— 取消后音频立即停止（增量 0 字节） |
| **工具定义与函数调用** | ✅ 通过 —— 模型真实发起 `look_at_screen` 调用，参数正确 |
| **上下文回注** | ✅ 通过 —— 回注"喜欢蓝色"历史，模型正确答出"蓝色"（30 分钟会话重建的前提） |
| 完整状态链 | ✅ `listening → thinking → speaking → listening → idle` |

### 2.2 本次发现并修复的 4 个真实缺陷

这些都是**只有接上真机才会暴露**的问题：

**① 音色 ID 是猜的，实际无效（我自己的 bug）**
`config.ts` 里我默认写了 `voice: "cangshubao"`。实测服务端直接拒绝：
```
{"message":"voice cangshubao is not valid","type":"request_params_invalid","code":"400"}
```
可用值为 `cixingnansheng`、`zhengpaiqingnian`。**已修正为实测可用值。**

**② 工具定义在连接前下发，被静默丢弃**
日志实证：`[Realtime] 发送失败（未连接）: session.update` —— 17 个工具定义全部丢失，模型无工具可调。
原因：`startSession()` 在 `connect()` 之前就 push 工具。
**已修正为：在收到 `session.created` 之后再下发工具。**

**③ 打断时"没有可取消的响应"被当成故障**
日志实证：用户打断若恰无进行中的响应，服务端回
```
invalid_request_error: no ongoing response to cancel
```
状态机被误推到 `error`。**已修正为：识别并忽略这条正常回执。**

**④ 端点与模型必须成对配置，混搭会 404**
实测矩阵：

| 模型 | `/v1` | `/step_plan/v1` |
|---|---|---|
| `stepaudio-3-realtime-preview` | ✅ | ❌ 404 |
| `stepaudio-2.5-realtime` | — | ✅ |
| `step-5-preview`（视觉） | ✅ | ✅ |

**已修正为：实时语音走 `/v1`，视觉走 `/step_plan/v1`。**

### 2.3 两处我此前的判断需要更正

| 事项 | 我之前的判断 | 实测事实 |
|---|---|---|
| `step_plan` 通道 | 在第二轮审查报告中判为"官方文档中不存在、疑似猜测" | **该通道真实可用**，有独立模型清单（10 个，与 `/v1` 的 36 个不同）。**原判断有误，特此更正。** |
| 视觉模型 ID | 曾填 `step-1o-turbo-vision` | 官方 chat completions 图像示例用 **`step-5-preview`**；已实测可用 |

> 教训：我自己也犯了"猜接口"的错（音色、视觉模型 ID）。已在 `docs/ORB_CHANGES.md` 与本文档记录，并新增可复现的探测脚本 `scripts/probe-*.mjs`，避免后续再靠猜。

---

## 三、8 个任务的最终状态

| 任务 | 交付物 | 审查结论 |
|---|---|---|
| **T01** Orb 集成路径定案 | 方案书 + `ORB_CHANGES.md` + 4 张环境验证截图 | ✅ **通过** |
| **T02** 架构设计与模块拆分 | 52.9 KB 架构文档 | ⚠️ **部分整改**（详见第五节） |
| **T03** Electron 壳与球体渲染 | 完整工程 + README + 阶段报告 + 7 张六态截图 | ✅ **通过** |
| **T04** 聊天面板与六态状态机 | 面板 + 状态机 + 调试面板 + 说明 + 报告 + 8 张截图 + 验收 JSON（5 项全过）+ 15 项单测 | ✅ **通过** |
| **T05** 实时语音链路 | 源码 + 说明 + 报告 | ✅ 已合并进最终工程并通过真实联调 |
| **T06** MCP 集成与工具调用 | 客户端 + 4 份文档 + 验证记录 + 审计日志 | ✅ **通过**（真实调用 26 工具/暴露 12 个） |
| **T07** 屏幕视觉与桌面控制 | 截图/窗口/视觉/鼠标键盘/安全 6 模块 + 说明 + 报告 + **69 项单测全过** + 真实 API 坐标端到端验证 | ✅ **通过** |
| **T08** 打包 EXE 与交付文档 | 打包方案 + 阶段报告 + 本交付报告 | ✅ **通过** |

**整合成果**：8 个任务的代码已全部合并为单一可运行工程 `Jarvis/`，统一构建、统一打包、统一自测。

---

## 四、最终产品能力

### 已实测可用

- **悬浮球**：透明/无边框/置顶/可拖动，经 `app://` 加载（非 `file://`），WebGPU 渲染正常
- **六态视觉**：`idle / listening / thinking / executing / speaking / error` 全部可切换，颜色形态各异（截图 + 像素统计双证）
- **实时语音**：真实握手、真实音频往返、真实打断、服务端 VAD 断句
- **屏幕视觉**：截图 + 前台窗口识别 + 视觉理解（真实 API 验证：能识别图像内容并输出坐标）
- **鼠标键盘**：koffi + Win32 SendInput 双后端 + 坐标换算（22 项单测覆盖高 DPI/多显示器/越界）
- **工具执行**：MCP 真实拉起，发现 26 个工具、按白名单暴露 12 个、真实调用返回正确内容
- **安全机制**：危险命令识别、高风险强制确认、审计日志、全局急停（`Ctrl+Alt+X`）、首次授权门

### 权威的功能清单

```
17 个暴露给模型的工具（4 内置 + 12 MCP + 1 窗口控制）：
  look_at_screen, click_on_screen, type_text, press_keys, close_current_window,
  read_file, write_file, create_directory, list_directory, move_file,
  start_search, get_file_info, edit_block, start_process,
  read_process_output, interact_with_process, list_processes
```

---

## 五、遗留事项（如实列出）

### 5.1 T02 架构文档：四项整改只完成一项半

| 编号 | 问题 | 状态 |
|---|---|---|
| A1 | `canvasColor = [0,0,0,0]` 写法错误（源码类型是 hex 字符串，写数组会生成无效 CSS） | ❌ 未修 |
| A2 | 视觉端点未经核实写成确定事实 | ⚠️ 现已证实可用，但文档未标注核实过程 |
| A3 | 模块编号表/图对不上（图中无 M07、表中无 M00） | ⚠️ **只改一半**：图里加了 M00，表里仍无；M07 仍不在图中 |
| A4 | 依赖图存在反向依赖（`M_FSM --> M_OrbBridge` 形成双向环） | ❌ 未修，无例外说明 |
| A5–A7 | 无来源数字 / 与 WebGPU 相悖的 GPU 参数建议 / 笔误"事实事实" | ❌ 未修 |

**影响评估**：不影响最终产品运行（代码层已按正确做法实现），但文档作为设计基线仍有误导性。**建议修订后再归档。**

### 5.2 未完成的验证

| 项 | 原因 | 影响 |
|---|---|---|
| T07 三个示例场景的完整录屏 | 最后一次点击需人工在确认弹窗点"允许"（安全机制不允许无人值守点击） | 各环节已单独验证 + 真实 API 坐标端到端已验证；**需人工走一遍并录屏** |
| 真机高 DPI / 多显示器实测 | 本机为 96 DPI 单屏 | 已有 22 项合成用例覆盖，但缺真机数据 |
| Windows 11 验收 | 本机是 Windows 10 Build 19045 | 功能无实质差异，但**任务书目标平台是 Win11，建议在 Win11 复测** |

### 5.3 需要用户决策

1. **D3 屏幕视觉实现方式**：已按任务书推荐方案 (a)（主进程原生实现）落地，**未经用户正式确认**。
2. **视觉模型定稿**：当前 `step-5-preview`（实测可用）；`step-3.7-flash` 亦可用。
3. **代码签名**：未签名，安装会触发 SmartScreen 警告。
4. **模型下线风险**：默认用 `stepaudio-3-realtime-preview`（限免预览版，官方声明限免结束后下线）。已做成可配置项。

### 5.4 协作冲突提醒

`Jarvis/` 工程在本次整合期间**有多个会话并行修改**（T04、T07 执行方均在改 `src/main/*`）。我已做兼容性合并（保留双方字段），`tsc` 与全部测试在合并后的代码树上通过。但**建议确认是否还有在跑的会话**，避免后续覆盖。

---

## 六、使用方式

1. 双击 `release\Jarvis-Setup-1.0.0-x64.exe` 安装（SmartScreen 警告点"更多信息 → 仍要运行"）
2. 启动后右下角出现悬浮球 + 聊天面板
3. 面板底部「设置与状态」→ 填入 **StepFun API Key**（或设环境变量 `STEPFUN_API_KEY`）
4. 点「启动语音会话」→ 授权麦克风 → 直接说话
5. 工具执行需额外配置**允许访问的目录白名单**（留空则禁用，这是刻意设计）

**当前已配置好可直接运行**（Key 存在 `%APPDATA%\jarvis-desktop-assistant\config.json`，未进代码/日志）。

---

## 七、交付物清单

```
Jarvis/
├── release/Jarvis-Setup-1.0.0-x64.exe     安装包（119 MB）
├── src/main/                              19 个主进程模块
├── src/preload/index.ts                   受限 API 桥
├── src/renderer/{orb,panel}.html          球体页 + 聊天面板（含 Web Audio）
├── scripts/                               4 个构建脚本 + 6 个验证/探测脚本
├── docs/
│   ├── 安装说明.md / 使用说明.md / 构建说明.md
│   ├── ORB_CHANGES.md                     对 Orb 的改动逐行留痕（合规必需）
│   ├── 验收证据.md
│   └── evidence/                          8 份原始验证日志
├── assets/                                8 张六态球体与面板截图
└── vendor/orb/                            Orb 源码（受控 fork）
```

---

## 八、结论

**产品已可交付且可直接运行。** 8 个任务全部产出并把代码合并为单一工程，关键链路（球体渲染、六态、实时语音、屏幕视觉、工具执行、安全机制、打包）**全部经过真机 + 真实 API 验证**，累计通过 100+ 项检查（含 84 项单元测试、24 项真机验收）。

本次接上密钥后**额外发现并修复了 4 个真实缺陷**，其中"音色 ID 猜错"和"工具在连接前下发被丢弃"这两个如果不做真机联调，会一直潜伏到用户第一次使用才爆发 —— 这也印证了 T07 坚持要真实场景验证的价值。

剩余问题集中在**文档层面（T02 的四项整改）**和**需人工完成的录屏**，不影响产品功能。
